# DocuChat — RAG Chatbot Dependencies (free stack)
# Python 3.11+ required
# Install with: pip install -r requirements.txt

# --- Web Framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# --- LangChain Core ---
langchain==0.3.9
langchain-core==0.3.21
langchain-community==0.3.9
langchain-text-splitters==0.3.2

# --- Free LLM: Groq (sign up at console.groq.com, free tier) ---
groq==0.9.0
httpx==0.27.2

# --- Free local embeddings: sentence-transformers ---
# Downloads all-MiniLM-L6-v2 (~90 MB) on first run, cached locally.
# No API key, no cost, runs on CPU.
huggingface-hub==0.24.0

# --- Vector Store ---
faiss-cpu==1.14.3

# --- PDF Parsing ---
PyMuPDF==1.24.13

# --- Utilities ---
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1
numpy==1.26.4
